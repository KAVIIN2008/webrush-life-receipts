export function Footer() {
  return (
    <footer className="border-t border-white/10 py-8 text-xs leading-6 text-white/50">
      <p>Connections and patterns shown here are derived from observable fields in the supplied dataset. Pattern labels describe activity structure, not personality or intent.</p>
    </footer>
  )
}
